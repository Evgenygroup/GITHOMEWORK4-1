import { RevDirective } from './rev.directive';

describe('RevDirective', () => {
  it('should create an instance', () => {
    const directive = new RevDirective();
    expect(directive).toBeTruthy();
  });
});
